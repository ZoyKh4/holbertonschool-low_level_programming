#include <stdio.h>

/**
 * main - thorfinn
 * description: "trust the process"
 */

int main(void)
{
char letter;
for (letter = 'a'; letter <= 'z'; letter++)
{
	putchar(letter);
}
putchar('\n');
	return (0);
}
